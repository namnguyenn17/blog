export type Article = {
  title: string;
  coverImage: string;
};

export type Views = {
  total: number;
};

export type Reactions = {
  like_count: number;
  love_count: number;
  clap_count: number;
  party_count: number;
};
